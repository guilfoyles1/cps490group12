//- views/new_chat.pug
doctype html
html
  head
    title Start New Chat
    link(rel="stylesheet", href="/css/styles.css")
  body
    h1 Start a New Chat
    form#new-chat-form(action="/chat/new" method="post")
      label(for="recipients") Enter recipient usernames (comma-separated if multiple):
      input#recipients(type="text" name="recipients" required placeholder="Recipient usernames (e.g. user1, user2)")
      button(type="submit") Create Chat

    //- Error message placeholder
    if error
      p.error-message(style="color: red;") #{error}
